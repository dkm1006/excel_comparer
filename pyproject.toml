[project]
name = "excel-comparer"
version = "0.1.0"
description = "Compare Excel workbooks against a golden file."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openpyxl>=3.1",
]

[project.scripts]
excel-comparer = "excel_comparer.cli:main"
